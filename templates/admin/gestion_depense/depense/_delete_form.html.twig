<form method="post" action="{{ path('admin_gestion_depense_depense_delete', {'id': depense.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depense.id) }}">
    <button class="btn">Delete</button>
</form>
