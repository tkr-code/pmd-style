{% import "macros/button.html.twig" as buttons %}
<form method="post" class="d-inline-block" action="{{ path('admin_cahier_charge_delete', {'id': cahier_charge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cahier_charge.id) }}">
    {{ buttons.delete('Delete'|trans) }}
</form>
