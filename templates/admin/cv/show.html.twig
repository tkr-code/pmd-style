{% extends 'base.html.twig' %}

{% block title %}Cv{% endblock %}

{% block body %}
    <h1>Cv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cv.description }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ cv.poste }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cv.email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ cv.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ cv.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cv_index') }}">back to list</a>

    <a href="{{ path('admin_cv_edit', {'id': cv.id}) }}">edit</a>

    {{ include('admin_cv/_delete_form.html.twig') }}
{% endblock %}
