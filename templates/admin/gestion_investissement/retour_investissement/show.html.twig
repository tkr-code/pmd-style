{% extends 'base.html.twig' %}

{% block title %}RetourInvestissement{% endblock %}

{% block body %}
    <h1>RetourInvestissement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ retour_investissement.id }}</td>
            </tr>
            <tr>
                <th>Montant_recu</th>
                <td>{{ retour_investissement.montantRecu }}</td>
            </tr>
            <tr>
                <th>Date_reception</th>
                <td>{{ retour_investissement.dateReception ? retour_investissement.dateReception|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EstRecuperer</th>
                <td>{{ retour_investissement.estRecuperer ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_gestion_investissement_retour_investissement_index') }}">back to list</a>

    <a href="{{ path('admin_gestion_investissement_retour_investissement_edit', {'id': retour_investissement.id}) }}">edit</a>

    {{ include('admin_gestion_investissement_retour_investissement/_delete_form.html.twig') }}
{% endblock %}
