<form method="post" action="{{ path('admin_gestion_investissement_retour_investissement_delete', {'id': retour_investissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retour_investissement.id) }}">
    <button class="btn">Delete</button>
</form>
