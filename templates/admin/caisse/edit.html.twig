{% extends 'admin/base.html.twig' %}
{% import "macros/link.html.twig" as links %}

{% block title %}Edit Caisse {{app.request.get('id') }} {% endblock %}

{% block body %}
<div class="card card-outline card-success">
    <h1>Edit Caisse {{ app.request.get('id')}} </h1>
    <div class="card-body">
        {{ include('admin/caisse/_form.html.twig', {'button_label': 'Update'}) }}
    
        {{ links.back(path('admin_caisse_index')) }}
    
        {{ include('admin/caisse/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
