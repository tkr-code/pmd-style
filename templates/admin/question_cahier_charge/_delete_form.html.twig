<form method="post" action="{{ path('admin_question_cahier_charge_delete', {'id': question_cahier_charge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question_cahier_charge.id) }}">
    <button class="btn">Delete</button>
</form>
