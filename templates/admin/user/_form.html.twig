{{ form_start(form) }}
    {# <div id="socials" data-prototype="" data-index=""></div> #}
    {{ form_widget(form) }}
    <hr>
    <button type="button" class="btn btn-primary" id="new-social">New Social</button>
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form,{render_rest:false}) }}
