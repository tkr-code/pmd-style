{% extends 'admin/base.html.twig' %}

{% block title %}FormationOption{% endblock %}

{% block body %}
    <h1>FormationOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation_option.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formation_option.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ formation_option.contenu|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_formation_option_index') }}">back to list</a>

    <a href="{{ path('app_admin_formation_option_edit', {'id': formation_option.id}) }}">edit</a>

    {{ include('admin/formation_option/_delete_form.html.twig') }}
{% endblock %}
