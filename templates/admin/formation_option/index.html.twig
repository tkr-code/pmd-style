{% extends 'admin/base.html.twig' %}

{% block title %}FormationOption index{% endblock %}

{% block body %}
    <h1>FormationOption index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                {# <th>Contenu</th> #}
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formation_option in formation_options %}
            <tr>
                <td>{{ formation_option.id }}</td>
                <td>{{ formation_option.titre }}</td>
                {# <td>{{ formation_option.contenu|raw }}</td> #}
                <td>
                    <a href="{{ path('app_admin_formation_option_show', {'id': formation_option.id}) }}">show</a>
                    <a href="{{ path('app_admin_formation_option_edit', {'id': formation_option.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_formation_option_new') }}">Create new</a>
{% endblock %}
