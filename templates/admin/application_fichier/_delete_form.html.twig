<form method="post" action="{{ path('app_admin_application_fichier_delete', {'id': application_fichier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ application_fichier.id) }}">
    <button class="btn">Delete</button>
</form>
