{% extends 'base.html.twig' %}

{% block title %}RendezVous index{% endblock %}

{% block body %}
    <h1>RendezVous index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateCreation</th>
                <th>DateRdv</th>
                <th>Lieu</th>
                <th>Heure</th>
                <th>Objet</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rendez_vou in rendez_vouses %}
            <tr>
                <td>{{ rendez_vou.id }}</td>
                <td>{{ rendez_vou.dateCreation ? rendez_vou.dateCreation|date('Y-m-d') : '' }}</td>
                <td>{{ rendez_vou.dateRdv ? rendez_vou.dateRdv|date('Y-m-d') : '' }}</td>
                <td>{{ rendez_vou.lieu }}</td>
                <td>{{ rendez_vou.heure }}</td>
                <td>{{ rendez_vou.objet }}</td>
                <td>{{ rendez_vou.description }}</td>
                <td>
                    <a href="{{ path('admin_gestion_rendez_vous_rendez_vous_show', {'id': rendez_vou.id}) }}">show</a>
                    <a href="{{ path('admin_gestion_rendez_vous_rendez_vous_edit', {'id': rendez_vou.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_gestion_rendez_vous_rendez_vous_new') }}">Create new</a>
{% endblock %}
