{% extends 'base.html.twig' %}

{% block title %}PaiementFormationDispensee{% endblock %}

{% block body %}
    <h1>PaiementFormationDispensee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement_formation_dispensee.id }}</td>
            </tr>
            <tr>
                <th>DatePaiement</th>
                <td>{{ paiement_formation_dispensee.datePaiement ? paiement_formation_dispensee.datePaiement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paiement_formation_dispensee.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_gestion_formation_paiement_formation_dispensee_index') }}">back to list</a>

    <a href="{{ path('admin_gestion_formation_paiement_formation_dispensee_edit', {'id': paiement_formation_dispensee.id}) }}">edit</a>

    {{ include('admin_gestion_formation_paiement_formation_dispensee/_delete_form.html.twig') }}
{% endblock %}
