{% extends 'base.html.twig' %}

{% block title %}PaiementFormationDispensee index{% endblock %}

{% block body %}
    <h1>PaiementFormationDispensee index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DatePaiement</th>
                <th>Montant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for paiement_formation_dispensee in paiement_formation_dispensees %}
            <tr>
                <td>{{ paiement_formation_dispensee.id }}</td>
                <td>{{ paiement_formation_dispensee.datePaiement ? paiement_formation_dispensee.datePaiement|date('Y-m-d') : '' }}</td>
                <td>{{ paiement_formation_dispensee.montant }}</td>
                <td>
                    <a href="{{ path('admin_gestion_formation_paiement_formation_dispensee_show', {'id': paiement_formation_dispensee.id}) }}">show</a>
                    <a href="{{ path('admin_gestion_formation_paiement_formation_dispensee_edit', {'id': paiement_formation_dispensee.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_gestion_formation_paiement_formation_dispensee_new') }}">Create new</a>
{% endblock %}
