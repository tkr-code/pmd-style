<form method="post" action="{{ path('admin_gestion_formation_centre_formation_delete', {'id': centre_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centre_formation.id) }}">
    <button class="btn">Delete</button>
</form>
