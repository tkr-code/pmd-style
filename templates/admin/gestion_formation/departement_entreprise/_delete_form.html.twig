<form method="post" action="{{ path('admin_gestion_formation_departement_entreprise_delete', {'id': departement_entreprise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departement_entreprise.id) }}">
    <button class="btn">Delete</button>
</form>
