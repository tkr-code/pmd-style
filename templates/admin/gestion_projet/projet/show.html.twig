{% extends 'base.html.twig' %}

{% block title %}Projet{% endblock %}

{% block body %}
    <h1>Projet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projet.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ projet.designation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ projet.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ projet.type }}</td>
            </tr>
            <tr>
                <th>ValeurTotal</th>
                <td>{{ projet.valeurTotal }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ projet.dateDebut ? projet.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFinPrevu</th>
                <td>{{ projet.dateFinPrevu ? projet.dateFinPrevu|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFinRealisation</th>
                <td>{{ projet.dateFinRealisation ? projet.dateFinRealisation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ projet.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_gestion_projet_projet_index') }}">back to list</a>

    <a href="{{ path('admin_gestion_projet_projet_edit', {'id': projet.id}) }}">edit</a>

    {{ include('admin_gestion_projet_projet/_delete_form.html.twig') }}
{% endblock %}
