{% extends 'base.html.twig' %}

{% block title %}Tache index{% endblock %}

{% block body %}
    <h1>Tache index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Designation</th>
                <th>Description</th>
                <th>Etat</th>
                <th>DateAchevement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tache in taches %}
            <tr>
                <td>{{ tache.id }}</td>
                <td>{{ tache.designation }}</td>
                <td>{{ tache.description }}</td>
                <td>{{ tache.etat }}</td>
                <td>{{ tache.dateAchevement ? tache.dateAchevement|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('admin_gestion_projet_tache_show', {'id': tache.id}) }}">show</a>
                    <a href="{{ path('admin_gestion_projet_tache_edit', {'id': tache.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_gestion_projet_tache_new') }}">Create new</a>
{% endblock %}
