<form method="post" action="{{ path('admin_gestion_projet_avance_paiement_delete', {'id': avance_paiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avance_paiement.id) }}">
    <button class="btn">Delete</button>
</form>
