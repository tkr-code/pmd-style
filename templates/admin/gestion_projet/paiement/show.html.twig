{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>DatePaiement</th>
                <td>{{ paiement.datePaiement ? paiement.datePaiement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ paiement.dateCreation ? paiement.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>MontantTotal</th>
                <td>{{ paiement.montantTotal }}</td>
            </tr>
            <tr>
                <th>MontantVerse</th>
                <td>{{ paiement.montantVerse }}</td>
            </tr>
            <tr>
                <th>MontantDu</th>
                <td>{{ paiement.montantDu }}</td>
            </tr>
            <tr>
                <th>EstAcheve</th>
                <td>{{ paiement.estAcheve ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>ModePaiement</th>
                <td>{{ paiement.modePaiement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_gestion_projet_paiement_index') }}">back to list</a>

    <a href="{{ path('admin_gestion_projet_paiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('admin_gestion_projet_paiement/_delete_form.html.twig') }}
{% endblock %}
