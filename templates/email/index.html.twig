{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
<div class="container my-5">
    <h1>Visualisation des emails</h1>
    <div class="list-group">
        {% for item in paths %}
        <a href="{{ path(item.path) }}" class="list-group-item list-group-item-action">{{item.name}}</a>
    {% endfor %}
    </div>
</div>
{% endblock %}
